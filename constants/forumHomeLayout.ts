/**
 * ForumHome — `Main Content` / `Container` (Inspect Figma).
 *
 * - Lista vertical (chips, fixado, posts): **padding 16** + **gap 12** entre filhos.
 * - Fila de chips: **gap 12** horizontal; **paddingLeft 16** (top/right/bottom 0 no frame de 60px).
 */
export const FORUM_HOME_MAIN_CONTENT_PAD = 16;

/** Gap vertical entre chips, card fixado e cada post (mesmo valor em todo o stack) */
export const FORUM_HOME_MAIN_CONTENT_GAP = 12;

/** Gap horizontal entre pills “Geral / Técnico / …” */
export const FORUM_HOME_CHIP_ROW_GAP = 12;
