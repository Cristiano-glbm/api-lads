import { bundledImageSource } from '@/utils/bundledImageSource';

/** Ícones de estado nos cartões “Minhas requisições” (Figma). */
export const SERVICE_STATUS_CONCLUIDO = bundledImageSource(
  require('../assets/images/service-status-concluido.png'),
);
export const SERVICE_STATUS_HOURGLASS = bundledImageSource(
  require('../assets/images/service-status-hourglass.png'),
);
export const SERVICE_STATUS_ORCAMENTO = bundledImageSource(
  require('../assets/images/service-status-orcamento.png'),
);
